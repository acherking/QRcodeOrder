<%= form_for(@authentication) do |f| %>
  <% if @authentication.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@authentication.errors.count, "error") %> prohibited this authentication from being saved:</h2>

      <ul>
      <% @authentication.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :authentication_str %><br>
    <%= f.text_field :authentication_str %>
  </div>
  <div class="field">
    <%= f.label :table_id %><br>
    <%= f.text_field :table_id %>
  </div>
  <div class="field">
    <%= f.label :statu %><br>
    <%= f.check_box :statu %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
